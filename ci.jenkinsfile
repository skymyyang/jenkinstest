#!groovy


String buildShell = "${env.buildShell}"
pipeline{
  agent { node { label "build-01"}}
  
  stages{
    stage("build"){
      steps{
      
           script{
              mvnHome = tool "M2"
             sh "${mvnHome}/bin/mvn ${buildShell}"
         
         
         }
        
      
      }
    
    
    }
  
  
  }
}
